{
  "name": "Japanese AI Outbound Caller",
  "build": { "dockerfile": "../Dockerfile", "context": ".." },
  "forwardPorts": [8000],
  "postCreateCommand": "pip install -e '.[dev,voice]'",
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python", "charliermarsh.ruff"]
    }
  }
}
